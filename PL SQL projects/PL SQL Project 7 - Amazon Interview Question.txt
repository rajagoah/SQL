SET SERVEROUTPUT ON
CREATE OR REPLACE PROCEDURE AMAZON_INTERVIEW_2
IS
    V_STATES STATES.STATES%TYPE;
BEGIN
    SELECT STATES INTO V_STATES 
    FROM STATES WHERE STATES = 'NJ';
    DBMS_OUTPUT.PUT_LINE(V_STATES);
END;

SET SERVEROUTPUT ON
BEGIN
    AMAZON_INTERVIEW_2;
END;
