SET SERVEROUTPUT ON
DECLARE 
    CURSOR C1 IS
    SELECT COL1, COL2 FROM TB;
    V_COL1 TB.COL1%TYPE;
    V_COL2 TB.COL2%TYPE;
BEGIN
    OPEN C1;
    FETCH C1 INTO V_COL1, V_COL2;
    CLOSE C1;
    
    DBMS_OUTPUT.PUT_LINE(V_COL1||' '||V_COL2);
END;