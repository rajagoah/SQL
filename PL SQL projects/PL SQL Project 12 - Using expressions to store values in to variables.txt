--USING EXPRESSION TO ASSIGN VALUES TO VARIABLES
SET SERVEROUTPUT ON
DECLARE
    V_VAR1 NUMBER;
    V_VAR2 NUMBER;
BEGIN
    V_VAR1 := 100;
    V_VAR2 := (V_VAR1*10)/15;
    DBMS_OUTPUT.PUT_LINE(V_VAR2);
END;